import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  depletionSection: {
    marginTop: 12,
    gap: 8,
  },
  nodeDepletedText: {
    color: "#FF6B6B",
    fontSize: 14,
    fontWeight: "600",
    textAlign: "center",
    marginTop: 4,
  },
});

export default styles;
